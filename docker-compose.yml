version: '3.8'

services:
  web:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./exports:/app/exports
      - ./sessions:/app/sessions
    env_file:
      - .env
    restart: unless-stopped
